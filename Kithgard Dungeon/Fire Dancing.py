# Le code s'exécute normalement dans l'ordre où il est écrit.
# Les boucles permettent de répéter un bloc de code plusieurs fois.
# Tu dois appuyer sur tab (ou 4 fois sur espace) pour indenter une ligne dans la boucle.

while True:
    hero.moveRight()
    # Ajouter ici une commande moveLeft à la boucle
    hero.moveLeft()
    
